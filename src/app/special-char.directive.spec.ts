import { SpecialCharDirective } from './special-char.directive';

describe('SpecialCharDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialCharDirective();
    expect(directive).toBeTruthy();
  });
});
